PARA INSTALAR O PACOTE E RODAR O NODE/TESTES:

1. cd backend
2. instalação
3. npm start ou npx poku

PACOTES: npm install express cors dotenv mssql multer pg bcrypt cloudinary sanitize-html express-validator jsonwebtoken quibble express-async-errors --save-dev start-server-and-test

PACOTES E DEPENDÊNCIAS:

npm install -D nodemon
npm install node-mocks-http --save-dev